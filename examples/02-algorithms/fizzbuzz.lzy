# FizzBuzz: the classic first algorithm.

for n in range(1, 21)
    if n % 15 == 0
        say "FizzBuzz"
    else if n % 3 == 0
        say "Fizz"
    else if n % 5 == 0
        say "Buzz"
    else
        say n
